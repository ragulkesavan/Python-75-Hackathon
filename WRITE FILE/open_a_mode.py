'''"a" mode will not delete all existing the contents in the file ,
    if the file contains existing contents then the new entries are appened to the last of file 
    and so all data are not lost and data are written as appended'''
#opening in "w" mode
f=open("lyrics.txt","a")
